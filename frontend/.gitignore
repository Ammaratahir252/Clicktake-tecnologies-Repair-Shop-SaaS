# Dependency directories
node_modules/
jspm_packages/

# Next.js build output
.next/
out/

# Environment variables (CRITICAL: Never upload these)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS metadata
.DS_Store
Thumbs.db

# IDE settings
.vscode/
.idea/
.vercel

# Local backup snapshots — superseded by MongoDB-backed backups (see lib/backup.ts);
# these only ever existed as local dev artifacts, never a real backup strategy on Vercel.
backups/

# TypeScript build info
*.tsbuildinfo
